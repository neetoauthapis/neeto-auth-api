---
title: "Workspace subdomain"
description: "Learn how to find your workspace subdomain."
---

## What is a subdomain?

A subdomain is the part of your workspace URL that comes before the main domain. For example, in the URL:

```
https://spinkart.neetoauth.com
```

**spinkart** is the subdomain.

This subdomain is crucial for making API requests, as it identifies your specific workspace within NeetoAuth.

## Step-by-step instructions

<Steps>
  <Step title="Locate your workspace URL">
    You can usually find this in your browser's address bar when logged into your workspace.

    Example:
    ```
    https://your-subdomain.neetoauth.com
    ```
  </Step>

  <Step title="Identify the main domain">
    Our main domain is: `neetoauth.com`
  </Step>

  <Step title="Extract the part before the main domain">
    Everything before `neetoauth.com` is your subdomain.

    **Example:**
    - URL: `https://green-earth.neetoauth.com`
    - Subdomain: `green-earth`
  </Step>
</Steps>

## Using Your Subdomain in API Requests

Once you've identified your subdomain, you'll use it in all API requests. The base URL format is:

```
https://{your-subdomain}.neetoauth.com/api/external/v2
```

For example, if your subdomain is `mycompany`, your API base URL would be:

```
https://mycompany.neetoauth.com/api/external/v2
```

## Common Examples

Here are some common subdomain patterns:

| Type       | Example URL                                  | Subdomain        |
| ---------- | -------------------------------------------- | ---------------- |
| Company    | `https://acmecorp.neetoauth.com`       | `acmecorp`       |
| Department | `https://marketing.neetoauth.com`      | `marketing`      |
| Project    | `https://product-launch.neetoauth.com` | `product-launch` |
